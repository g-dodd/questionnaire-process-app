import { HasReportPipe } from './has-report.pipe';

describe('HasReportPipe', () => {
  it('create an instance', () => {
    const pipe = new HasReportPipe();
    expect(pipe).toBeTruthy();
  });
});
