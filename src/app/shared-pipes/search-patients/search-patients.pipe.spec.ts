import { SearchPatientsPipe } from './search-patients.pipe';

describe('SearchPatientsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchPatientsPipe();
    expect(pipe).toBeTruthy();
  });
});
